# 当前验证快照

日期：2026-08-16
软件状态：`2.0.2` 正式稳定版；科学证据 `PROVISIONAL`
引擎 SHA-256：`5a62ebcff640be863522642f292abf6f5beea7a4c90c294dc740c321f9f7e75e`

## 已通过

- 源码树与干净便携发布副本完整回归：`196 passed, 7 subtests passed`。
- `optimize-airfoil-cgrid` 及发布副本均通过 `quick_validate.py`；NACA0012 与
  NACA2418/B2 两个独立真实任务完成 Skill 前向验证。
- 新/旧 JobSpec、可配置任务级单元上限、`1..20` 候选预算、无 AI 暂停、非法候选
  拒绝、状态恢复、运行级源码隔离和 SSH 网格检查点签名均通过契约测试。
- 原 48,458 单元 C-grid 参数与生成器保持不变；新增的 `mesh-fallback` 只有在 AI
  名额耗尽且无可验收 AI 候选时开放一次。它忽略运行级补丁，仍执行同一硬门禁、
  100 次试算和显式 AI 验收；前置调用、重复调用和硬失败后继续均被测试拒绝。
- 高 AR 且近正交的壁面单元不会仅因 AR 被拒绝；低 AR 但折叠、扭曲或低正交质量
  的单元仍触发硬失败。
- NACA0012 尖尾缘真实 Fluent 2025 R1：验收候选为 53,576 个纯四边形单元，
  Fluent 最小 OQ `0.234686`，最大 skewness `0.694598`，100 次一阶 SST 试算通过；
  OQ 软警告已由 AI 显式签署。
- `NACA2418.dat` 真实复现 B2：旧默认 48,458 候选的最小 OQ 为 `0.171429`、
  非壁面最大 AR 为 `1193.086`，100 次试算通过，但没有按最高 OQ 自动接受。
  第二候选的 Fluent 启动超时被记录为失败并消耗预算；工作流继续实际评估第三候选。
  第三候选为 52,692 个纯四边形单元，最小 OQ `0.171416`、最大 skewness
  `0.890339`、非壁面最大 AR `721.136`，无负/零体积或连接错误且 100 次试算通过。
  它与旧默认互不支配，已凭区域物理与帕累托理由显式验收。
- 快速几何集 NACA0018、2414、63-412、6413、6412 均经 Fluent 2025 R1 读入，
  全部为纯四边形且无负/零体积或连接错误；闭合尖尾缘解析及前缘/远场区域标签错误
  已修正。NACA6413/6412 的参数候选确认剩余热点位于法向保护层至 C-wrap 的交界行。
- 快速几何集的推荐候选依次为：NACA0018 `48,210 / OQ 0.332063 / skew 0.630403`；
  NACA2414 `48,210 / 0.274251 / 0.819729`；NACA63-412
  `53,972 / 0.301393 / 0.803455`；NACA6413 `48,210 / 0.135369 / 0.910266`；
  NACA6412 `53,204 / 0.099125 / 0.935044`。后两者虽可由 Fluent 正常读取，仍有
  明显的法向保护层至 C-wrap 软质量热点，不能宣称方法已经完全优化。
- 最新对话复审后补充 OQ/skewness 分位数与坏单元占比、上下翼面 BL 分区、wake core、
  局部面积退化比和方向偏差。NACA0018 的 48,458 单元烟雾验证得到上下 BL 各 6,177
  个单元，尾迹方向 P95 偏差 `5.040807°`；这些数值均为软诊断，不替代 Fluent 门禁。
- 便携构建成功：`RELEASE_READY`；源码与发布副本的引擎
  清单哈希一致，发布副本 Skill 有效。

## 明确未执行或不构成结论

- 正式发布前的简要回归没有重新执行完整基线与五次翼型伴随优化；相关科学结论仍沿用已保存证据，不因软件发布状态而升级。
- 100 次一阶试算只证明单候选在给定物理下可以正常计算，不证明正式收敛、预测精度、
  网格无关性或优化成功。
- 三档 GCI 仍只有历史网格结构检查，没有三档收敛 Cd/Cl 解，不能计算可信 GCI。
- 尚未在第二台干净 Windows/SSH worker 上完成断网、断线恢复和真实许可证实机验收。

因此任何 AI 仍必须报告 `PROVISIONAL`，不得声称 `PRODUCTION_QUALIFIED` 或“优化成功”。
